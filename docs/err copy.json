分析小红书网页向服务器提交的行为数据，找出所有行为上报接口和上报内容。

步骤：
1. 调用 get_captured_requests，urlFilter 设为空，resourceType 设为 XHR，includeBody 设为 true，limit 设为 50
2. 从捕获的请求中筛选出所有 POST 请求，特别是包含以下关键词的 URL：
   - report / track / log / metric / behavior / profile / shield / webprofile
   - 注意 /api/sec/v1/shield/webprofile 这个端点
3. 对每个行为上报请求，提取并分析：
   - 请求 URL 和路径
   - 请求体（JSON 结构、字段含义）
   - 请求头中与行为相关的字段（x-b3-traceid, x-rap-param, x-s, x-s-common）
   - 响应状态和内容
4. 重点关注：哪些用户行为会触发上报（如页面加载、滚动、点击、停留时长、鼠标移动）
5. 分析上报数据中是否包含：设备指纹、鼠标轨迹、滚动深度、停留时长、页面可见性变化
6. 调用 execute_js 检查 window 对象上是否有行为采集相关的全局变量或事件监听器：
   - 检查 window.addEventListener 的事件类型列表
   - 检查 document 上 visibilitychange、scroll、click、mousemove 等事件监听器
   - 检查是否有 MutationObserver 或 IntersectionObserver 用于行为采集

输出：
- 行为上报接口清单（URL、方法、请求体结构）
- 上报触发条件（什么操作会触发上报）
- 上报数据字段含义对照表
- 哪些行为数据影响风控判定（如停留时长过短 = 机器人特征）



分析小红书 x-rap-param 请求头的生成机制，找出如何动态生成该参数。

步骤：
1. 调用 get_captured_requests，urlFilter 设为 "edith.xiaohongshu.com"，resourceType 设为 XHR，includeBody 设为 true
2. 从捕获请求的 requestHeaders 中提取 x-rap-param 的完整值，记录其 Base64 编码内容
3. 调用 execute_js，在页面中解码 x-rap-param 的结构：
   ```javascript
   // 获取最近请求的 x-rap-param
   var entries = performance.getEntriesByType('resource').filter(e => e.name.includes('edith.xiaohongshu.com'));
   // 检查 window 上是否有 rap 相关全局变量
   var rapKeys = Object.keys(window).filter(k => k.toLowerCase().includes('rap') || k.toLowerCase().includes('param'));
   // 检查 localStorage 中是否有 rap 相关数据
   var lsKeys = [];
   for (var i = 0; i < localStorage.length; i++) {
     var key = localStorage.key(i);
     if (key.toLowerCase().includes('rap') || key.toLowerCase().includes('sign') || key.toLowerCase().includes('sdt')) {
       lsKeys.push({ key, value: localStorage.getItem(key).substring(0, 500) });
     }
   }
   return { rapKeys, lsKeys, entryCount: entries.length };

```
4.调用 get_captured_requests，urlFilter 设为 "so.xiaohongshu.com"，对比搜索请求和详情请求的 x-rap-param 是否不同
5.如果 x-rap-param 在不同请求中值不同，说明是动态生成的；如果相同，说明是静态的
6.调用 fetch_script_source 获取 vendor-dynamic.js 源码（URL 从页面 script 标签中获取），搜索 "rap" 或 "x-rap" 关键词
7.调用 execute_js，在页面中搜索 x-rap-param 的设置位置：

```JavaScript

// 检查 fetch/XHR 拦截器
var origFetch = window.fetch.toString();
var origXHR = window.XMLHttpRequest.prototype.open.toString();
var origSetHeader = window.XMLHttpRequest.prototype.setRequestHeader.toString();
// 检查是否有 axios 拦截器
var hasAxios = typeof window.axios !== 'undefined';
// 检查 sdt_source_storage_key 配置
var sdtConfig = localStorage.getItem('sdt_source_storage_key');
return { origFetch: origFetch.substring(0, 500), origSetHeader: origSetHeader.substring(0, 500), hasAxios, sdtConfig: sdtConfig ? JSON.parse(sdtConfig) : null };
调用 execute_js，尝试触发一次 API 请求并观察 x-rap-param 的生成：

JavaScript

// 拦截 setRequestHeader 捕获 x-rap-param
var captured = [];
var orig = XMLHttpRequest.prototype.setRequestHeader;
XMLHttpRequest.prototype.setRequestHeader = function(name, value) {
  if (name.toLowerCase() === 'x-rap-param') {
    captured.push({ name, value: value.substring(0, 200), time: Date.now() });
  }
  return orig.apply(this, arguments);
};
// 等待 3 秒后返回捕获结果
return new Promise(resolve => {
  setTimeout(() => {
    XMLHttpRequest.prototype.setRequestHeader = orig;
    resolve({ captured, count: captured.length });
  }, 3000);
});
```
输出：

x-rap-param 是静态还是动态（不同请求中值是否变化）
x-rap-param 的生成位置（哪个 JS 文件、哪个函数）
x-rap-param 的数据结构（解码后的字段含义）
x-rap-param 与设备指纹/会话的关系
是否可以通过 execute_js 调用页面函数动态生成新的 x-rap-param
搜索 API 和详情 API 的 x-rap-param 是否可以互换使用